class Metric:
    def __init__(self, instance_id, cpu_usage, memory_usage, cdisk_usage, ddisk_usage):
        self.Instance_id = instance_id
        self.cpu_usage = cpu_usage
        self.memory_usage = memory_usage
        self.cdisk_usage = cdisk_usage
        self.ddisk_usage = ddisk_usage

